# studywithme
1.- add env variables inside backend/ folder
with the following keys:
CONTENT_SERVICE_KEY=
PORT=

2.- run backend
cd backend
npm run dev

3.- run flutter
flutter pub get
flutter run -d chrome
